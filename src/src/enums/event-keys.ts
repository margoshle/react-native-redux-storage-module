const prefix = 'redux-storage-module/';
export enum EventKeys {
  LOST_CONNECT = prefix + 'lost-connect-to-internet',
  UNEXPECTED_ERROR = prefix + 'unexpected-error',
  EXP_TOKEN = prefix + 'exp-token',
  LOGOUT = prefix + 'logout-application',
  SHOW_MESSAGE_ERROR = prefix + 'show-message-error',
}
